// Idêntico ao enum `ResourceType` do schema.prisma original — espelha
// `TipoRecurso` em src/lib/types.ts.
export enum ResourceType {
  CHAIR = 'CHAIR',
  ROOM = 'ROOM',
  OFFICE = 'OFFICE',
  EQUIPMENT = 'EQUIPMENT',
  TABLE = 'TABLE',
  VEHICLE = 'VEHICLE',
  OTHER = 'OTHER',
}
