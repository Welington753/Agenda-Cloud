// Idêntico ao enum `ServiceModality` do schema.prisma original — espelha
// `ModalidadeAtendimento` em src/lib/types.ts.
export enum ServiceModality {
  IN_PERSON = 'IN_PERSON',
  REMOTE = 'REMOTE',
  HOME = 'HOME',
}
