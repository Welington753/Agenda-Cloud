// Idêntico ao enum `TenantStatus` do schema.prisma original.
export enum TenantStatus {
  TRIAL = 'TRIAL',
  ACTIVE = 'ACTIVE',
  SUSPENDED = 'SUSPENDED',
  PAST_DUE = 'PAST_DUE',
  CANCELED = 'CANCELED',
}
