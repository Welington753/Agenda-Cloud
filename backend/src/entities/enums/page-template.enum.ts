// Idêntico ao enum `PageTemplate` do schema.prisma original — espelha
// `ModeloPaginaPublica` (`"classico" | "moderno"`) de src/lib/types.ts.
export enum PageTemplate {
  CLASSIC = 'CLASSIC',
  MODERN = 'MODERN',
}
