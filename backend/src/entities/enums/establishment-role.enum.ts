// Idêntico ao enum `EstablishmentRole` do schema.prisma original — espelha
// `PapelEstabelecimento` em src/lib/types.ts.
export enum EstablishmentRole {
  DONO = 'DONO',
  GERENTE = 'GERENTE',
  RECEPCIONISTA = 'RECEPCIONISTA',
  PROFISSIONAL = 'PROFISSIONAL',
}
